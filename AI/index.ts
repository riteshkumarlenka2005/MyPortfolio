/**
 * AI Communication Engine — Public Interface
 *
 * This is the ONLY file the rest of the project should import from.
 * Everything else inside AI/ is internal.
 *
 * Usage:
 *   import { CommunicationEngine } from '../AI/index';
 *   <CommunicationEngine />
 */

export { CommunicationEngine } from './components/CommunicationEngine';
